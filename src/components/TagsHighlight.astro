---
import Tag from '@react/TagSelector/Tag';
// import { getTags } from '@/utils/content';

// const mapped = await getTags();

// const select = Array.from(mapped.entries())
// 	.sort((a, b) => b[1].length - a[1].length)
// 	.slice(0, 3)
// 	.map(([tag]) => tag)
// 	.sort((a, b) => a.localeCompare(b));
---

<div class="flex flex-row gap-4 py-4">
	<!-- {select.map((tag) => {
        return (
            <Tag
                tag={tag}
                href={`/built/tags/${tag}`}
                client:load
            />
        );
    })} -->
	<Tag disabled tag="machine learning" href="#" />
	<Tag disabled tag="web development" href="#" />
</div>
