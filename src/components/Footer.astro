---
import { SERVER_URL_LIVE } from 'astro:env/server';
import GridMain from '@react/GridMain';

const today = new Date();

interface Props {
	className?: string;
}
const { className } = Astro.props;
---

<footer
	class={`${className} h-48 w-full items-center border-t border-mono-600`}
>
	<!-- <div
		class="mx-auto flex w-full max-w-4xl flex-row flex-wrap items-center gap-4 p-4 md:h-20"
	>
		<div class="flex w-fit flex-row gap-2 text-mono-500 dark:text-mono-600">
			<div>made in berlin</div>
			<div>&copy;</div>
			<div class="font-mono">{today.getFullYear()}</div>
		</div>
		<div class="flex flex-row gap-4">
			<a href={'/legal/imprint'}>imprint</a>
			<a href={'/legal/privacy'}>privacy</a>
		</div>
	</div> -->
	<GridMain>
		<div class="flex">Test</div>
		<div class="flex">Test</div>
		<div class="flex">Test</div>
	</GridMain>
</footer>
